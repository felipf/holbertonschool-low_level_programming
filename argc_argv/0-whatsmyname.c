#include <stdio.h>
/**
 * main - name followed by a new line.
 * @argc: n entered in cmd line
 * @argv: add string values entered
 * Return: 0.
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
